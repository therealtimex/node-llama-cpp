name: Test
on:
  push:
    branches:
      - master
      - beta
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install modules
        run: npm ci --ignore-scripts
      - name: ESLint
        run: npx eslint
      - name: TypeScript validity
        run: npm run test:typescript

  compile-docs:
    name: Test docs compilation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
          fetch-depth: 0
          fetch-tags: true
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install modules
        run: npm ci --ignore-scripts
      - name: Build
        run: npm run build
      - name: Resolve tracked llama.cpp release
        id: llamaRelease
        run: echo "release=$(node -p 'require(\"./llama/binariesGithubRelease.json\").release')" >> "$GITHUB_OUTPUT"
      - name: Download tracked llama.cpp release
        env:
          CI: true
        run: node ./dist/cli/cli.js source download --release "${{ steps.llamaRelease.outputs.release }}" --skipBuild --noBundle --noUsageExample --updateBinariesReleaseMetadataAndSaveGitBundle
      - name: Compile docs
        run: npm run docs:build
